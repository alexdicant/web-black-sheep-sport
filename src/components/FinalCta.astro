<section class="final-cta" aria-labelledby="final-cta-title">
  <h2 class="final-cta__headline" id="final-cta-title">¿Ya viste tus fotos?</h2>
  <p class="final-cta__subtitle">Busca tu evento, selecciona tus fotos y completa la compra por WhatsApp.</p>
  <a class="final-cta__button" href="/#tu-evento">
    Buscar mis fotos
  </a>
</section>

<style>
  .final-cta {
    padding: var(--space-section-block) var(--container-padding);
    background: var(--color-ink);
    text-align: center;
  }

  .final-cta__headline {
    color: var(--color-text-inverse);
    font-size: var(--font-size-2xl);
    line-height: 1.05;
    text-transform: uppercase;
    text-wrap: balance;
    margin-bottom: var(--space-3);
  }

  .final-cta__subtitle {
    max-width: var(--measure-prose);
    margin-inline: auto;
    font-size: var(--font-size-sm);
    color: var(--color-gray-300);
    margin-bottom: var(--space-6);
  }

  .final-cta__button {
    display: inline-block;
    text-decoration: none;
    font-family: var(--font-family-display);
    font-weight: 800;
    font-size: var(--font-size-sm);
    letter-spacing: 0.02em;
    text-transform: uppercase;
    background: var(--color-action);
    color: var(--color-action-text);
    padding: var(--space-4) var(--space-5);
    border-radius: var(--radius-sm);
    transition: background 0.18s ease, transform 0.18s ease;
  }

  .final-cta__button:active {
    transform: translateY(1px);
  }

  @media (hover: hover) and (pointer: fine) {
    .final-cta__button:hover {
      /* Ya está en volt sobre fondo negro: invertir a ink+volt se perdería
         contra la sección. En vez de eso, pasa a blanco. */
      background: var(--color-surface);
      transform: translateY(-2px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .final-cta__button,
    .final-cta__button:hover,
    .final-cta__button:active {
      transition: none;
      transform: none;
    }
  }

  @media (min-width: 900px) {
    .final-cta__button {
      padding: var(--space-4) var(--space-6);
    }
  }
</style>
