---
interface Step {
  number: string;
  title: string;
  description: string;
}

const steps: Step[] = [
  {
    number: "01",
    title: "Encuentra",
    description: "Busca tu evento y entra a su galería.",
  },
  {
    number: "02",
    title: "Selecciona",
    description: "Toca las fotos que quieras comprar. Puedes añadir o quitar fotos de tu selección.",
  },
  {
    number: "03",
    title: "Compra",
    description: "Pulsa «Comprar seleccionadas» y te llevamos a WhatsApp con tus fotos ya identificadas.",
  },
];
---

<section class="how-to-buy" aria-labelledby="how-to-buy-title">
  <h2 class="how-to-buy__label" id="how-to-buy-title">Cómo comprar</h2>

  <ol class="how-to-buy__list">
    {
      steps.map((step) => (
        <li class="how-to-buy__step">
          <span class="how-to-buy__number">{step.number}</span>
          <div>
            <p class="how-to-buy__title">{step.title}</p>
            <p class="how-to-buy__description">{step.description}</p>
          </div>
        </li>
      ))
    }
  </ol>
</section>

<style>
  .how-to-buy {
    padding-top: var(--space-6);
    padding-bottom: var(--space-section-block);
    padding-inline: var(--container-padding);
    max-width: var(--container-max-width);
    margin: 0 auto;
  }

  .how-to-buy__label {
    font-family: var(--font-family-display);
    font-weight: 800;
    font-size: var(--font-size-sm);
    letter-spacing: 0.09em;
    color: var(--color-gray-500);
    text-transform: uppercase;
    margin-bottom: var(--space-5);
  }

  .how-to-buy__list {
    display: flex;
    flex-direction: column;
    border-bottom: 1px solid var(--color-gray-200);
  }

  .how-to-buy__step {
    display: flex;
    align-items: flex-start;
    gap: var(--space-4);
    padding: var(--space-4) 0;
    border-top: 1px solid var(--color-gray-200);
  }

  .how-to-buy__number {
    font-family: var(--font-family-display);
    font-weight: 800;
    font-size: var(--font-size-2xl);
    color: var(--color-gray-400);
    line-height: 1;
  }

  .how-to-buy__title {
    font-weight: 700;
    font-size: var(--font-size-base);
    margin-bottom: var(--space-1);
  }

  .how-to-buy__description {
    max-width: var(--measure-prose);
    font-size: var(--font-size-sm);
    color: var(--color-gray-500);
  }

  @media (min-width: 900px) {
    .how-to-buy__list {
      flex-direction: row;
      gap: var(--space-7);
      padding-top: var(--space-6);
      border-top: 1px solid var(--color-gray-200);
      border-bottom: none;
    }

    .how-to-buy__step {
      flex: 1;
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-3);
      padding: 0;
      border-top: none;
    }
  }
</style>
