---
import { Image } from "astro:assets";
import muestra1 from "../assets/photos/DSC00134.webp";
import muestra2 from "../assets/photos/DSC03575.webp";
import muestra3 from "../assets/photos/DSC09177.webp";
import muestra4 from "../assets/photos/DSC08303.webp";
import muestra5 from "../assets/photos/DSC06183-Mejorado-NR.webp";
import muestra6 from "../assets/photos/DSC09992.webp";

const photos = [
  { src: muestra1, alt: "Atleta lanzando un golpe con guantes de boxeo" },
  { src: muestra2, alt: "Atleta sentada en un retrato de estudio, con un cinturón de levantamiento junto a ella" },
  { src: muestra3, alt: "Atleta dando una patada alta de kickboxing hacia un costal de boxeo" },
  { src: muestra4, alt: "Atleta en pleno peso muerto con barra cargada" },
  { src: muestra5, alt: "Espalda de un atleta haciendo dominadas en barra" },
  { src: muestra6, alt: "Atleta de pie junto a la barra, recuperando el aliento después del levantamiento" },
];
---

<section class="gallery" id="muestra" aria-labelledby="gallery-title">
  <h2 class="gallery__label" id="gallery-title">Muestra</h2>

  <div class="gallery__grid">
    {
      photos.map((photo) => (
        <div class="gallery__cell">
          <Image
            class="gallery__image"
            src={photo.src}
            alt={photo.alt}
            loading="lazy"
            widths={[400, 800, 1200]}
            sizes="(min-width: 900px) 16vw, 50vw"
          />
        </div>
      ))
    }
  </div>
</section>

<style>
  .gallery {
    padding: var(--space-section-block) var(--container-padding);
    max-width: var(--container-max-width);
    margin: 0 auto;
  }

  .gallery__label {
    font-family: var(--font-family-display);
    font-weight: 800;
    font-size: var(--font-size-sm);
    letter-spacing: 0.09em;
    color: var(--color-gray-500);
    text-transform: uppercase;
    margin-bottom: var(--space-5);
  }

  .gallery__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-1);
  }

  .gallery__cell {
    overflow: hidden;
  }

  .gallery__image {
    width: 100%;
    height: auto;
    aspect-ratio: 2 / 3;
    object-fit: cover;
    transition: transform 0.35s cubic-bezier(0.2, 0.75, 0.2, 1);
  }

  @media (min-width: 900px) {
    .gallery__grid {
      grid-template-columns: repeat(6, 1fr);
      gap: var(--space-2);
    }
  }

  @media (min-width: 900px) and (hover: hover) and (pointer: fine) {
    .gallery__cell:hover .gallery__image {
      transform: scale(1.045);
    }
  }
</style>
